% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html.R
\name{write_pdf}
\alias{write_pdf}
\title{Write a tsg table (or list of tables) to a PDF file}
\usage{
write_pdf(
  data,
  path,
  ...,
  title = NULL,
  subtitle = NULL,
  source_note = NULL,
  footnotes = NULL,
  separate_files = TRUE,
  names_separator = "__",
  facade = get_tsg_facade(which = "html")
)
}
\arguments{
\item{data}{A \code{tsg} or data frame, or a named list of them.}

\item{path}{File path for the PDF output. A \code{.pdf} extension is added if missing.
When \code{data} is a list and \code{separate_files = TRUE}, this is used as a directory.}

\item{...}{Additional arguments passed to \code{tsg_to_gt()}.}

\item{title}{Optional title string (overrides data attribute).}

\item{subtitle}{Optional subtitle string.}

\item{source_note}{Optional source note string.}

\item{footnotes}{Optional character vector of footnotes.}

\item{separate_files}{Logical. When \code{data} is a list, \code{TRUE} (default) saves
each table as a separate PDF file inside a subdirectory; \code{FALSE} merges them into
a single PDF (requires \pkg{qpdf}).}

\item{names_separator}{Column name separator for spanners. Default \code{"__"}.}

\item{facade}{Styling options. Defaults to the global tsg facade.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Saves tables as PDF using \code{gt::gtsave()}, which requires the \pkg{webshot2}
package (and a Chromium installation reachable by \pkg{chromote}).
}
\details{
When \code{data} is a named list, the default (\code{separate_files = TRUE}) writes
each table to its own \code{.pdf} file inside a directory. Set
\code{separate_files = FALSE} to merge all tables into a single PDF (requires the
\pkg{qpdf} package).
}
