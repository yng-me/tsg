% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequency.R
\name{add_column_total}
\alias{add_column_total}
\title{Add a Total Row to a Frequency Table}
\usage{
add_column_total(
  data,
  position = "bottom",
  colname = "percent",
  label_total = "Total"
)
}
\arguments{
\item{data}{A data frame containing a `category` column and a `frequency` column (and optionally a percent/proportion column).}

\item{position}{Character. Where to place the total row: `"top"` or `"bottom"`.}

\item{colname}{Character. Name of the column to sum along with frequency (usually `"percent"` or `"proportion"`).}

\item{label_total}{Character. Label to assign to the total row/category (default is `"Total"`).}
}
\value{
A data frame with a total row added. The object includes an attribute `"total"` with metadata about the operation.
}
\description{
Adds a total row to a frequency table, computing the sum of the `frequency` column and (optionally) a percentage or proportion column.
Handles different types of category variables (e.g., labelled, factor, character, numeric).
}
\examples{
df <- data.frame(
  category = factor(c("A", "B", "C")),
  frequency = c(10, 20, 30),
  percent = c(16.7, 33.3, 50)
)

add_column_total(df, position = "bottom", colname = "percent", label_total = "Total")



}
